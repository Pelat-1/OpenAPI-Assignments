openapi: 3.0.2
paths:
  beers:
    get:
      operationId: getBeerListV1
      summary: Get Beer list
      description: Get a page of **Beer**s in the system
      tags:
        - beers
      parameters:
        - $ref: './Components.yaml#/components/parameters/pageNumber'
        - $ref: './Components.yaml#/components/parameters/pageSize'
      responses:
        200:
          description: Get Beer list
          content:
            application/json:
              schema:
                $ref: './Components.yaml#/components/schemas/beerPagedList'
        401:
          description: User is not allowed to retrieve Beer list
        404:
          description: Beer list empty
    post:
      operationId: newBeerV1
      summary: New Beer
      description: Create a new **Beer**
      tags:
        - beers
      requestBody:
        $ref: "./Components.yaml#/components/requestBodies/newBeer"
      responses:
        201:
          description: Beer created
          headers:
            location:
              $ref: './Components.yaml#/components/headers/beerLocation'
        401:
          description: User is not allowed to create a new Customer
  beersBeerId:
    get:
      operationId: getBeerByIdV1
      summary: Get Beer by ID
      description: Retrieve a single **Beer** in the system
      tags:
        - beers
      parameters:
        - $ref: './Components.yaml#/components/parameters/beerId'
      responses:
        200:
          description: Get Beer from beerId
          content:
            application/json:
              schema:
                $ref: './Components.yaml#/components/schemas/beer'
        401:
          description: User is not allowed to retrieve the Beer
        404:
          description: Beer not found
    put:
      operationId: updateBeerV1
      summary: Update Beer
      description: Update **Beer** by ID
      tags:
        - beers
      parameters:
        - $ref: './Components.yaml#/components/parameters/beerId'
      requestBody:
        $ref: "./Components.yaml#/components/requestBodies/newBeer"
      responses:
        204:
          description: Beer updated
        401:
          description: User is not allowed to update the Beer
        404:
          description: Beer not found
  customers:
    get:
      operationId: getCustomerListV1
      summary: Get Customer list
      description: Get a page of **Customer**s in the system
      tags:
        - customers
      parameters:
        - $ref: './Components.yaml#/components/parameters/pageNumber'
        - $ref: './Components.yaml#/components/parameters/pageSize'
      responses:
        200:
          description: Get Customer list
          content:
            application/json:
              schema:
                $ref: './Components.yaml#/components/schemas/customerPagedList'
        401:
          description: User is not allowed to retrieve Customer list
        404:
          description: Customer list empty
    post:
      operationId: newCustomerV1
      summary: New Customer
      description: Create a new **Customer**
      tags:
        - customers
      requestBody:
        $ref: "./Components.yaml#/components/requestBodies/newCustomer"
      responses:
        201:
          description: Customer created
          headers:
            location:
              $ref: './Components.yaml#/components/headers/customerLocation'
        401:
          description: User is not allowed to create a new Customer
  customersCustomerId:
    get:
      operationId: getCustomerByIdV1
      summary: Get Customer by ID
      description: Retrieve a single **Customer** in the system
      tags:
        - customers
      parameters:
        - $ref: './Components.yaml#/components/parameters/customerId'
      responses:
        200:
          description: Get Customer from customerId
          content:
            application/json:
              schema:
                $ref: './Components.yaml#/components/schemas/customer'
        401:
          description: User is not allowed to retrieve the Customer
        404:
          description: Customer not found
    put:
      operationId: updateCustomerV1
      summary: Update Customer
      description: Update **Customer** by ID
      tags:
        - customers
      parameters:
        - $ref: './Components.yaml#/components/parameters/customerId'
      requestBody:
        $ref: "./Components.yaml#/components/requestBodies/newCustomer"
      responses:
        204:
          description: Customer updated
        401:
          description: User is not allowed to update the Customer
        404:
          description: Customer not found